# ───── Global ignores ───────────────────────
**/.circleci
**/.editorconfig
**/.dockerignore
**/.git
**/.DS_Store
**/.vscode
**/node_modules

# ───── Project-specific artifacts ───────────
data-node
meili_data*
# librechat*          ← removed so librechat.yaml is copied into the image
Dockerfile*
docs

# ───── Catch-all for dotfiles ───────────────
.*
